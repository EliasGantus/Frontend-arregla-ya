# ArreglaYa Bug Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corregir 9 bugs identificados en la revisión del proyecto ArreglaYa: 3 en el backend (lógica de negocio) y 6 en el frontend (visual + UX).

**Architecture:** Los cambios de backend se aplican sobre `C:\Users\agust\Backend-arregla-ya` y los de frontend sobre `C:\Users\agust\Frontend-arregla-ya`. El backend requiere una migración de Prisma (tasks 1-2). El servidor de backend debe reiniciarse después de cada cambio de backend; el frontend con Vite recarga solo.

**Tech Stack:** Backend: Node/Express/TypeScript/Prisma/PostgreSQL/multer. Frontend: React 19/TypeScript/Vite/TailwindCSS/React Hook Form/Zod.

---

## Archivos modificados

### Backend (`C:\Users\agust\Backend-arregla-ya`)
| Archivo | Acción | Bug |
|---------|--------|-----|
| `prisma/schema.prisma` | Modify | #7, #2 |
| `src/lib/serializers.ts` | Modify | #7, #2, #10 |
| `src/modules/bookings/bookings.routes.ts` | Modify | #7, #8 |
| `src/modules/professionals/professionals.routes.ts` | Modify | #8 |
| `src/modules/quotes/quotes.routes.ts` | Modify | #10 |
| `src/modules/service-requests/service-requests.routes.ts` | Modify | #2 |
| `src/modules/uploads/uploads.routes.ts` | Create | #2 |
| `src/routes/index.ts` | Modify | #2 |
| `src/app.ts` | Modify | #2 |

### Frontend (`C:\Users\agust\Frontend-arregla-ya`)
| Archivo | Acción | Bug |
|---------|--------|-----|
| `src/shared/types/api.ts` | Modify | #7, #10 |
| `src/shared/types/contracts.ts` | Modify | #6 |
| `src/app/layouts/app-shell.tsx` | Modify | #4 |
| `src/features/auth/pages/login-page.tsx` | Modify | #5 |
| `src/features/service-requests/pages/service-requests-page.tsx` | Modify | #6, #2 |
| `src/features/quotes/pages/quotes-page.tsx` | Modify | #10 |
| `FIXES.md` | Create | docs |

---

## Task 1: Backend — Prisma schema: agregar COMPLETED a ServiceRequest y campo photos

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] **Step 1: Agregar COMPLETED al enum ServiceRequestStatus y photos a ServiceRequest**

Editar `C:\Users\agust\Backend-arregla-ya\prisma\schema.prisma`:

```prisma
enum ServiceRequestStatus {
  DRAFT
  OPEN
  QUOTED
  ASSIGNED
  COMPLETED
  CANCELLED
}
```

```prisma
model ServiceRequest {
  id          String               @id @default(uuid())
  title       String
  description String
  status      ServiceRequestStatus @default(OPEN)
  city        String
  zone        String
  budget      String?
  photos      String[]
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @updatedAt
  clientId    String
  categoryId  String
  client      User                 @relation("ClientServiceRequests", fields: [clientId], references: [id])
  category    Category             @relation(fields: [categoryId], references: [id])
  quotes      Quote[]
  bookings    Booking[]
}
```

- [ ] **Step 2: Ejecutar migración**

Correr en `C:\Users\agust\Backend-arregla-ya`:
```
npx prisma migrate dev --name add-completed-status-and-photos
```
Expected: `The following migration(s) have been created and applied...`

---

## Task 2: Backend — Serializers: soportar COMPLETED y photos

**Files:**
- Modify: `src/lib/serializers.ts`

- [ ] **Step 1: Actualizar serviceRequestStatusMap**

En `src/lib/serializers.ts`, reemplazar:

```typescript
const serviceRequestStatusMap: Record<
  ServiceRequestStatus,
  'draft' | 'open' | 'quoted' | 'assigned' | 'cancelled'
> = {
  DRAFT: 'draft',
  OPEN: 'open',
  QUOTED: 'quoted',
  ASSIGNED: 'assigned',
  CANCELLED: 'cancelled',
};
```

con:

```typescript
const serviceRequestStatusMap: Record<
  ServiceRequestStatus,
  'draft' | 'open' | 'quoted' | 'assigned' | 'completed' | 'cancelled'
> = {
  DRAFT: 'draft',
  OPEN: 'open',
  QUOTED: 'quoted',
  ASSIGNED: 'assigned',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
};
```

- [ ] **Step 2: Actualizar serializeServiceRequest para incluir photos**

Reemplazar la función `serializeServiceRequest`:

```typescript
export const serializeServiceRequest = (
  serviceRequest: ServiceRequest & { category: Pick<Category, 'id' | 'name' | 'slug'> },
) => ({
  id: serviceRequest.id,
  title: serviceRequest.title,
  description: serviceRequest.description,
  status: serviceRequestStatusMap[serviceRequest.status],
  category: serializeCategory(serviceRequest.category),
  city: serviceRequest.city,
  zone: serviceRequest.zone,
  budget: serviceRequest.budget ?? undefined,
  photos: serviceRequest.photos,
  createdAt: serviceRequest.createdAt.toISOString(),
});
```

---

## Task 3: Backend — Fix booking completion actualiza ServiceRequest

**Files:**
- Modify: `src/modules/bookings/bookings.routes.ts:240-245`

- [ ] **Step 1: Agregar bloque COMPLETED después del bloque CANCELLED**

En `bookings.routes.ts`, después de:
```typescript
    if (status === 'CANCELLED') {
      await prisma.serviceRequest.update({
        where: { id: current.serviceRequestId },
        data: { status: 'OPEN' },
      });
    }
```

Agregar:
```typescript
    if (status === 'COMPLETED') {
      await prisma.serviceRequest.update({
        where: { id: current.serviceRequestId },
        data: { status: 'COMPLETED' },
      });
    }
```

---

## Task 4: Backend — Fix conflict check usa ventana de tiempo (±2h)

**Files:**
- Modify: `src/modules/bookings/bookings.routes.ts:76-93`
- Modify: `src/modules/professionals/professionals.routes.ts:98-112`

- [ ] **Step 1: Actualizar ensureProfessionalAvailable en bookings.routes.ts**

Reemplazar la función completa:

```typescript
const ensureProfessionalAvailable = async (
  professionalId: string,
  scheduledAt: Date,
  excludeBookingId?: string,
) => {
  const windowStart = new Date(scheduledAt.getTime() - 2 * 60 * 60 * 1000);
  const windowEnd = new Date(scheduledAt.getTime() + 2 * 60 * 60 * 1000);

  const conflictingBooking = await prisma.booking.findFirst({
    where: {
      professionalId,
      scheduledAt: { gte: windowStart, lte: windowEnd },
      status: { in: activeBookingStatuses },
      id: excludeBookingId ? { not: excludeBookingId } : undefined,
    },
  });

  if (conflictingBooking) {
    throw new HttpError(409, 'El profesional no está disponible en ese horario.', 'BOOKING_CONFLICT');
  }
};
```

- [ ] **Step 2: Actualizar isProfessionalAvailable en professionals.routes.ts**

Reemplazar la función completa:

```typescript
const isProfessionalAvailable = async (professionalId: string, scheduledAt?: Date) => {
  if (!scheduledAt) {
    return true;
  }

  const windowStart = new Date(scheduledAt.getTime() - 2 * 60 * 60 * 1000);
  const windowEnd = new Date(scheduledAt.getTime() + 2 * 60 * 60 * 1000);

  const conflictingBooking = await prisma.booking.findFirst({
    where: {
      professionalId,
      scheduledAt: { gte: windowStart, lte: windowEnd },
      status: { in: [...activeBookingStatuses] },
    },
  });

  return !conflictingBooking;
};
```

---

## Task 5: Backend — Fix serializeQuote incluye serviceRequestTitle

**Files:**
- Modify: `src/modules/quotes/quotes.routes.ts:27-41` y `:73-90`
- Modify: `src/lib/serializers.ts:135-146`

- [ ] **Step 1: Actualizar GET /quotes/me para incluir serviceRequest**

En `quotes.routes.ts`, en el handler de `GET /quotes/me`, reemplazar el `prisma.quote.findMany`:

```typescript
    const quotes = await prisma.quote.findMany({
      where,
      include: {
        professional: {
          select: {
            id: true,
            fullName: true,
          },
        },
        serviceRequest: {
          select: {
            id: true,
            title: true,
          },
        },
      },
      orderBy: { createdAt: 'desc' },
    });
```

- [ ] **Step 2: Actualizar POST /service-requests/:id/quotes para incluir serviceRequest**

En `quotes.routes.ts`, en el handler del POST, reemplazar `prisma.quote.create`:

```typescript
    const quote = await prisma.quote.create({
      data: {
        serviceRequestId: payload.serviceRequestId,
        professionalId: request.auth!.userId,
        amount: payload.amount,
        message: payload.message,
        status: 'PENDING',
      },
      include: {
        professional: {
          select: {
            id: true,
            fullName: true,
          },
        },
        serviceRequest: {
          select: {
            id: true,
            title: true,
          },
        },
      },
    });
```

- [ ] **Step 3: Actualizar serializeQuote en serializers.ts**

Reemplazar la función completa:

```typescript
export const serializeQuote = (
  quote: Quote & {
    professional: Pick<User, 'id' | 'fullName'>;
    serviceRequest: Pick<ServiceRequest, 'id' | 'title'>;
  },
) => ({
  id: quote.id,
  serviceRequestId: quote.serviceRequestId,
  serviceRequestTitle: quote.serviceRequest.title,
  professionalId: quote.professionalId,
  professionalName: quote.professional.fullName,
  amount: quote.amount,
  status: quoteStatusMap[quote.status],
  message: quote.message,
  createdAt: quote.createdAt.toISOString(),
});
```

---

## Task 6: Backend — Endpoint de upload de imágenes con multer

**Files:**
- Create: `src/modules/uploads/uploads.routes.ts`
- Modify: `src/routes/index.ts`
- Modify: `src/app.ts`
- Modify: `src/modules/service-requests/service-requests.routes.ts`

- [ ] **Step 1: Instalar multer**

Correr en `C:\Users\agust\Backend-arregla-ya`:
```
npm install multer
npm install --save-dev @types/multer
```

- [ ] **Step 2: Crear directorio de uploads**

```
mkdir public\uploads
```

- [ ] **Step 3: Crear uploads.routes.ts**

Crear `src/modules/uploads/uploads.routes.ts`:

```typescript
import path from 'path';
import { Router } from 'express';
import multer from 'multer';

import { HttpError } from '../../lib/http-error.js';
import { authenticate } from '../../middleware/authenticate.js';

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => cb(null, 'public/uploads/'),
  filename: (_req, file, cb) => {
    const ext = path.extname(file.originalname);
    const name = `${Date.now()}-${Math.random().toString(36).slice(2)}${ext}`;
    cb(null, name);
  },
});

const upload = multer({
  storage,
  limits: { fileSize: 5 * 1024 * 1024 },
  fileFilter: (_req, file, cb) => {
    if (file.mimetype.startsWith('image/')) {
      cb(null, true);
    } else {
      cb(new HttpError(400, 'Solo se permiten imágenes.', 'INVALID_FILE_TYPE') as unknown as null, false);
    }
  },
});

export const uploadsRouter = Router();

uploadsRouter.post(
  '/uploads',
  authenticate,
  upload.single('file'),
  (request, response) => {
    if (!request.file) {
      response.status(400).json({ message: 'No se recibió ningún archivo.' });
      return;
    }
    response.status(201).json({ url: `/uploads/${request.file.filename}` });
  },
);
```

- [ ] **Step 4: Agregar static serving en app.ts**

En `src/app.ts`, agregar después de `app.use(express.json())`:

```typescript
  app.use('/uploads', express.static('public/uploads'));
```

El archivo completo queda:

```typescript
import cors from 'cors';
import express from 'express';

import { env } from './config/env.js';
import { errorHandler, notFoundHandler } from './middleware/error-handler.js';
import { apiRouter } from './routes/index.js';

export const createApp = () => {
  const app = express();

  app.use(
    cors({
      origin: env.CORS_ORIGIN.split(',').map((value) => value.trim()),
      credentials: true,
    }),
  );
  app.use(express.json());
  app.use('/uploads', express.static('public/uploads'));

  app.get('/health', (_request, response) => {
    response.json({
      status: 'ok',
      service: 'arreglaya-backend',
      timestamp: new Date().toISOString(),
    });
  });

  app.use(apiRouter);
  app.use(notFoundHandler);
  app.use(errorHandler);

  return app;
};
```

- [ ] **Step 5: Registrar uploadsRouter en routes/index.ts**

Agregar la importación y el uso:

```typescript
import { uploadsRouter } from '../modules/uploads/uploads.routes.js';
```

Y después de `apiRouter.use(docsRouter)`:
```typescript
apiRouter.use(uploadsRouter);
```

- [ ] **Step 6: Aceptar photos en POST /service-requests**

Leer `src/modules/service-requests/service-requests.routes.ts`. En `createSchema`, agregar:
```typescript
  photos: z.array(z.string()).max(4).optional(),
```

En el `prisma.serviceRequest.create`, agregar al `data`:
```typescript
  photos: payload.photos ?? [],
```

- [ ] **Step 7: Reiniciar el backend**

Detener el proceso y correr `npm run dev` nuevamente en `C:\Users\agust\Backend-arregla-ya`.

---

## Task 7: Frontend — Agregar COMPLETED a tipos y componentes

**Files:**
- Modify: `src/shared/types/api.ts`
- Modify: `src/features/service-requests/pages/service-requests-page.tsx`

- [ ] **Step 1: Agregar 'completed' a ServiceRequestStatus en api.ts**

Reemplazar:
```typescript
export type ServiceRequestStatus =
  | 'draft'
  | 'open'
  | 'quoted'
  | 'assigned'
  | 'cancelled';
```

con:
```typescript
export type ServiceRequestStatus =
  | 'draft'
  | 'open'
  | 'quoted'
  | 'assigned'
  | 'completed'
  | 'cancelled';
```

También agregar `photos` a `ServiceRequest` y `CreateServiceRequestInput`:

```typescript
export interface ServiceRequest {
  id: string;
  title: string;
  description: string;
  status: ServiceRequestStatus;
  category: CategorySummary;
  city: string;
  zone: string;
  budget?: string;
  photos: string[];
  createdAt: string;
}

export interface CreateServiceRequestInput {
  title: string;
  description: string;
  categoryId: string;
  city: string;
  zone: string;
  budget?: string;
  photos?: string[];
}
```

- [ ] **Step 2: Actualizar statusCopy, statusTone y requestProgress en service-requests-page.tsx**

Reemplazar los tres records:

```typescript
const statusCopy: Record<ServiceRequestStatus, string> = {
  draft: 'Borrador',
  open: 'Abierta',
  quoted: 'Cotizada',
  assigned: 'Asignada',
  completed: 'Completada',
  cancelled: 'Cancelada',
};

const statusTone: Record<ServiceRequestStatus, string> = {
  draft: 'bg-slate-100 text-slate-600',
  open: 'bg-accent-50 text-accent-700',
  quoted: 'bg-brand-50 text-brand-700',
  assigned: 'bg-emerald-50 text-emerald-700',
  completed: 'bg-green-100 text-green-800',
  cancelled: 'bg-red-50 text-red-700',
};

const requestProgress: Record<ServiceRequestStatus, string> = {
  draft: 'Datos iniciales',
  open: 'Esperando cotizaciones',
  quoted: 'Cotizaciones recibidas',
  assigned: 'Profesional asignado',
  completed: 'Trabajo completado',
  cancelled: 'Solicitud cancelada',
};
```

---

## Task 8: Frontend — Fix bottom nav muestra 4 ítems para profesional

**Files:**
- Modify: `src/app/layouts/app-shell.tsx:59-63` y `184`

- [ ] **Step 1: Simplificar visibleMobileItems para mostrar todos los ítems del rol**

Reemplazar el bloque `visibleMobileItems` (líneas 59-63):

```typescript
  const visibleMobileItems = mobileItems;
```

- [ ] **Step 2: Hacer el grid dinámico según cantidad de ítems**

En el `<nav>` del bottom bar (línea 184), reemplazar:
```tsx
        <div className="mx-auto grid max-w-md grid-cols-3 gap-2">
```

con:
```tsx
        <div className={`mx-auto grid max-w-md gap-2 ${visibleMobileItems.length >= 4 ? 'grid-cols-4' : 'grid-cols-3'}`}>
```

---

## Task 9: Frontend — Agregar link "¿Olvidaste tu contraseña?" en login

**Files:**
- Modify: `src/features/auth/pages/login-page.tsx`

- [ ] **Step 1: Agregar estado showForgotMessage**

En `LoginPage`, después de `const [errorMessage, setErrorMessage] = useState<string | null>(null);`, agregar:

```typescript
  const [showForgotMessage, setShowForgotMessage] = useState(false);
```

- [ ] **Step 2: Agregar el link después del campo Contraseña en el form de login**

En el form de login (`mode === 'login'`), después de la `<label>` de Contraseña (y antes del bloque de error), agregar:

```tsx
              <div className="text-right">
                <button
                  type="button"
                  className="text-xs text-slate-400 transition hover:text-slate-600"
                  onClick={() => setShowForgotMessage((prev) => !prev)}
                >
                  ¿Olvidaste tu contraseña?
                </button>
                {showForgotMessage ? (
                  <p className="mt-1 text-xs text-slate-500">
                    Esta función no está disponible aún. Contactá al administrador.
                  </p>
                ) : null}
              </div>
```

---

## Task 10: Frontend — Quitar campo descripción, usar title como description

**Files:**
- Modify: `src/shared/types/contracts.ts`
- Modify: `src/features/service-requests/pages/service-requests-page.tsx`

- [ ] **Step 1: Actualizar serviceRequestSchema en contracts.ts**

Reemplazar:
```typescript
export const serviceRequestSchema = z.object({
  title: z.string().min(4, 'Describe mejor el trabajo.'),
  description: z.string().min(12, 'Agrega más detalle para los profesionales.'),
  categoryId: z.string().min(1, 'Selecciona una categoría.'),
  city: z.string().min(2, 'Ingresa una ciudad valida.'),
  zone: z.string().min(2, 'Ingresa una zona o barrio.'),
  budget: z.string().optional(),
});
```

con:
```typescript
export const serviceRequestSchema = z.object({
  title: z.string().min(12, 'Describí mejor el problema (mín. 12 caracteres).'),
  categoryId: z.string().min(1, 'Selecciona una categoría.'),
  city: z.string().min(2, 'Ingresa una ciudad valida.'),
  zone: z.string().min(2, 'Ingresa una zona o barrio.'),
  budget: z.string().optional(),
});
```

- [ ] **Step 2: Quitar description del form en service-requests-page.tsx**

En `ServiceRequestsContent`:

2a. En `useForm defaultValues`, quitar `description: ''`.

2b. Quitar la `<label>` entera del campo Descripcion (las líneas que contienen el Textarea con `{...register('description')}`).

2c. En `reset()` dentro de `onSuccess`, quitar `description: ''`.

2d. Actualizar `submitRequest` para construir el payload explícitamente:

```typescript
  const submitRequest = (values: ServiceRequestFormValues) => {
    mutation.mutate({
      title: values.title,
      description: values.title,
      categoryId: values.categoryId,
      city: values.city,
      zone: values.zone,
      budget: values.budget,
    });
  };
```

2e. Cambiar el label del campo Titulo a `Describe tu problema` y actualizar el placeholder:

```tsx
            <label className="space-y-2 md:col-span-2">
              <span className="text-sm font-semibold text-slate-700">
                Describe tu problema
              </span>
              <Input
                error={errors.title?.message}
                placeholder="Ej: Instalación de aire acondicionado en Living"
                {...register('title')}
              />
            </label>
```

---

## Task 11: Frontend — Fix quote cards muestran título en vez de UUID

**Files:**
- Modify: `src/shared/types/api.ts`
- Modify: `src/features/quotes/pages/quotes-page.tsx`

- [ ] **Step 1: Agregar serviceRequestTitle a la interfaz Quote en api.ts**

Reemplazar:
```typescript
export interface Quote {
  id: string;
  serviceRequestId: string;
  professionalName: string;
  professionalId: string;
  amount: string;
  status: QuoteStatus;
  message: string;
  createdAt: string;
}
```

con:
```typescript
export interface Quote {
  id: string;
  serviceRequestId: string;
  serviceRequestTitle: string;
  professionalName: string;
  professionalId: string;
  amount: string;
  status: QuoteStatus;
  message: string;
  createdAt: string;
}
```

- [ ] **Step 2: Reemplazar {quote.serviceRequestId} en quotes-page.tsx**

Buscar (línea ~280):
```tsx
                  {quote.serviceRequestId}
```

Reemplazar con:
```tsx
                  {quote.serviceRequestTitle}
```

---

## Task 12: Frontend — Agregar upload de imágenes al formulario de solicitud

**Files:**
- Modify: `src/features/service-requests/pages/service-requests-page.tsx`

- [ ] **Step 1: Agregar estado y handler de upload**

Al inicio de `ServiceRequestsContent`, agregar:

```typescript
  const { accessToken } = useAuth();
  const [uploadedPhotos, setUploadedPhotos] = useState<string[]>([]);
  const [uploadError, setUploadError] = useState<string | null>(null);

  const handlePhotoUpload = async (event: React.ChangeEvent<HTMLInputElement>) => {
    const file = event.target.files?.[0];
    event.target.value = '';
    if (!file || uploadedPhotos.length >= 4) return;

    const formData = new FormData();
    formData.append('file', file);

    try {
      setUploadError(null);
      const res = await fetch(`${env.apiUrl}/uploads`, {
        method: 'POST',
        headers: { Authorization: `Bearer ${accessToken ?? ''}` },
        body: formData,
      });
      if (!res.ok) throw new Error('Error al subir imagen.');
      const data = (await res.json()) as { url: string };
      setUploadedPhotos((prev) => [...prev, `${env.apiUrl}${data.url}`]);
    } catch {
      setUploadError('No se pudo subir la imagen. Intentá de nuevo.');
    }
  };

  const removePhoto = (index: number) => {
    setUploadedPhotos((prev) => prev.filter((_, i) => i !== index));
  };
```

- [ ] **Step 2: Agregar sección de fotos al formulario**

Dentro del `<form>`, después del bloque de Ciudad/Zona y antes del `<div>` del botón submit, agregar:

```tsx
            <div className="space-y-2 md:col-span-2">
              <span className="text-sm font-semibold text-slate-700">
                Agregar imágenes del problema
              </span>
              <div className="flex flex-wrap gap-2">
                {uploadedPhotos.map((url, i) => (
                  <div
                    key={url}
                    className="relative h-16 w-16 overflow-hidden rounded-2xl border border-slate-200"
                  >
                    <img
                      alt={`Foto ${i + 1}`}
                      className="h-full w-full object-cover"
                      src={url}
                    />
                    <button
                      className="absolute right-0 top-0 flex h-5 w-5 items-center justify-center rounded-bl bg-red-500 text-xs font-bold text-white"
                      type="button"
                      onClick={() => removePhoto(i)}
                    >
                      ×
                    </button>
                  </div>
                ))}
                {uploadedPhotos.length < 4 ? (
                  <label className="flex h-16 w-16 cursor-pointer items-center justify-center rounded-2xl border-2 border-dashed border-slate-300 hover:border-accent-400">
                    <span className="text-2xl leading-none text-slate-400">+</span>
                    <input
                      accept="image/*"
                      className="hidden"
                      type="file"
                      onChange={(e) => void handlePhotoUpload(e)}
                    />
                  </label>
                ) : null}
              </div>
              {uploadError ? (
                <p className="text-xs text-red-600">{uploadError}</p>
              ) : null}
            </div>
```

- [ ] **Step 3: Pasar photos en submitRequest**

Actualizar `submitRequest` (modificada en Task 10) para incluir photos:

```typescript
  const submitRequest = (values: ServiceRequestFormValues) => {
    mutation.mutate({
      title: values.title,
      description: values.title,
      categoryId: values.categoryId,
      city: values.city,
      zone: values.zone,
      budget: values.budget,
      photos: uploadedPhotos,
    });
  };
```

- [ ] **Step 4: Limpiar photos en onSuccess**

En `onSuccess` del mutation, agregar:
```typescript
      setUploadedPhotos([]);
```

---

## Task 13: Crear FIXES.md

**Files:**
- Create: `C:\Users\agust\Frontend-arregla-ya\FIXES.md`

- [ ] **Step 1: Crear el archivo de documentación**

El archivo se crea y actualiza automáticamente al completar cada task. Ver `FIXES.md` en la raíz del repo frontend.

---

## Orden de ejecución recomendado

1. Tasks 1-6 (backend) → reiniciar servidor backend
2. Tasks 7-12 (frontend) → Vite recarga automáticamente
3. Task 13 (docs)
